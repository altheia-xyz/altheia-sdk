{
  "name": "@altheia-xyz/sdk",
  "version": "0.0.2",
  "description": "AI agents you can fire — Altheia SDK. Per-action policy, on-chain Swig session keys, one-click revoke. For AI agents on Solana.",
  "license": "Apache-2.0",
  "homepage": "https://docs.altheia.xyz",
  "repository": {
    "type": "git",
    "url": "https://github.com/altheia-xyz/altheia-sdk.git",
    "directory": "packages/sdk"
  },
  "keywords": ["solana", "ai", "agent", "iam", "swig", "altheia", "audit", "policy"],
  "type": "module",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": ["dist", "README.md", "LICENSE"],
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --clean",
    "dev": "tsup src/index.ts --format esm --dts --watch",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@altheia-xyz/types": "workspace:*",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "tsup": "^8.3.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
