{
  "name": "@altheia-xyz/example-sak-trader",
  "version": "0.0.1",
  "private": true,
  "description": "E2E-001: sample agent that uses the Solana Agent Kit through Altheia. Demonstrates the allow → deny → allow cycle with a real ALTHEIA_AGENT_ID against a running backend.",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "demo": "tsx src/index.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@altheia-xyz/sdk": "workspace:*",
    "@altheia-xyz/solana-agent-kit": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
